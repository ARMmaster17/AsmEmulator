#pragma once
#include "SFLAG_Register.h"
#include "RAM.h"
static class Opcodes
{
public:
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAD(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
	void AAA(SFLAG_Register* sfr, RAM* mem);
};

